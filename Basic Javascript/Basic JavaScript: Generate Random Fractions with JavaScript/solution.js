// Create a function randomFraction that returns a random number between 0 (inclusive) to 1 (exclusive)

const randomFraction = () => Math.random();

const randomFraction = () => Math.floor(Math.random() * 28);
